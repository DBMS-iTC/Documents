= collaborative PP-Module for DBMS Cryptographic Functions
:showtitle:
:toc: macro
:toclevels: 7
:sectnums:
:sectnumlevels: 7
:imagesdir: images
:icons: font
:doctype: book
:revnumber: 0.3
:revdate: 2026-02-23

:iTC-longname: Database Management Systems
:iTC-shortname: DBMS-iTC
:iTC-email: cm-itc-mailing-list@gmail.com
:iTC-website: https://github.com/DBMS-iTC
:iTC-GitHub: https://github.com/DBMS-iTC/DBMS-iTC.github.io
:base-pp: collaborative Protection Profile for Database Management Systems

:sectnums!:

== Acknowledgements
This collaborative Protection Profile Module (PP-Module) was developed by the {iTC-longname} international Technical Community (iTC) also known as {iTC-shortname} with representatives from industry, Government agencies, Common Criteria Test Laboratories, and members of academia.

*INDUSTRY*

_Vendors_

*COMMON CRITERIA TESTING LABORATORIES*

_ITSEF Name_

*GOVERNMENT AGENCIES*

_Government Agency Names_

=== Revision History

.Revision history
[%header,cols=".^1,.^2,.^4"]
|===

|Version
|Date
|Description

|0.1
|2026-01-25
|Initial Draft

|0.2
|2026-01-25
|Revised to act as a Consumer of the CC Crypto Catalogue; removed redundant algorithm definitions.

|0.3
|2026-02-23
|Structural completeness update: expanded PP-Module Base with allowed PP-Configurations and relationship diagram; added CC Conformance Claims section; added Organizational Security Policies (P.CRYPTOGRAPHIC_STANDARDS, P.KEY_LIFECYCLE); added OE.KEY_STORE_PROTECTION objective; expanded Security Rationale with full Assumptions-to-Objectives and Objectives-to-SFRs mapping tables and consistency summary; added Security Assurance Requirements section; added Extended Component Definitions appendix (FCS_CKM_EXT, FDP_DAR_EXT, FDP_ITC_EXT); added Consistency Rationale appendix; added SFR List appendix; added Global Dependency Resolution Summary appendix; added Optional Requirements appendix. Corrected Catalogue component references against CCDB-018 v1.0 (January 2025): FCS_CKM.4 replaced with FCS_CKM.6; FCS_COP.1/SKC replaced with FCS_COP.1/SKC and FCS_COP.1/AEAD; FCS_CKM.1/SKG renamed to FCS_CKM.1/SKG per CCDB-018 symmetric key generation component; TLS reference updated to Functional Package for TLS v2.1 (CC:2022).

|===

toc::[]

== Preface

This PP-Module, the *DBMS Cryptographic Functions Module*, extends the collaborative Protection Profile for Database Management Systems (cPP_DBMS) to support secure cryptographic implementations.

Unlike traditional PP-Modules, this module does not define Security Functional Requirements (SFRs) for specific cryptographic algorithms (e.g., AES, RSA, SHA). Instead, it *invokes the Common Criteria Cryptographic Catalogue* [<<Crypto_Catalog>>]. This module focuses specifically on the database-specific application of cryptography, including:

* *Key Hierarchy Management*: The management of Master Keys and Data Encryption Keys (DEKs).
* *Key Import/BYOK*: The secure import of keys from external entities.
* *Encryption Strategy*: The scope of data-at-rest encryption (Full Volume vs. Granular).

This module must be used in conjunction with the cPP_DBMS and the applicable selections from the Cryptographic Catalogue.

=== Objectives of Document
This document expresses the security functional requirements for database-specific cryptographic operations, referencing the standardized catalogue for algorithm implementations.

=== Scope of Document
The scope of this PP-Module within the development and evaluation process is described in the Common Criteria for Information Technology Security Evaluation, CC:2022. In particular, a PP-Module defines the IT security requirements of a generic type of TOE and specifies the functional security measures to be offered by that TOE to meet stated requirements [<<CC1>>, Section B.14].

=== Intended Readership
The target audiences of this PP-Module are developers, CC consumers, system integrators, evaluators and schemes.

Although the PP-Module may contain minor editorial errors, it is recognized as a living document and the iTC is dedicated to ongoing updates and revisions. Please report any issues to the {iTC-shortname}.

=== Related Documents

[bibliography]
* [#CC1]#[CC1]# Common Criteria for Information Technology Security Evaluation, Part 1: Introduction and General Model, CC:2022, Version 2022, Revision 1.
* [#CC2]#[CC2]# Common Criteria for Information Technology Security Evaluation, Part 2: Security Functional Components, CC:2022, Version 2022, Revision 1.
* [#CC3]#[CC3]# Common Criteria for Information Technology Security Evaluation, Part 3: Security Assurance Components, CC:2022, Version 2022, Revision 1.
* [#CEM]#[CEM]# Common Methodology for Information Technology Security Evaluation, CEM:2022.
* [#SD]#[SD]# Supporting Document - Evaluation Activities for DBMS Cryptographic Functions Module.
* [#Crypto_Catalog]#[Crypto_Catalog]# Specification of Functional Requirements for Cryptography (CCDB-018), Version 1.0, January 2025. Available at https://github.com/commoncriteria/crypto-catalog[commoncriteria/crypto-catalog] and https://www.commoncriteriaportal.org/files/ccfiles/CCDB-018-v1.0-2025-Jan-31-Final-Specification_of_Functional_Requirements_for_Cryptography.pdf[commoncriteriaportal.org].
* [#TLS_FP]#[TLS_FP]# Functional Package for Transport Layer Security (TLS), Version 2.1, 2025-08-25. Available at https://commoncriteria.github.io/tls/release-2.1/tls-release.html[commoncriteria.github.io/tls].
* [#cPP_DBMS]#[cPP_DBMS]# collaborative Protection Profile for Database Management Systems.
* [#DBMS_Cloud_MOD]#[DBMS_Cloud_MOD]# collaborative PP-Module for DBMS in the Cloud.

For more information, see the http://www.commoncriteriaportal.org/[Common Criteria Portal].

:sectnums:
== PP-Module Introduction

=== PP-Module Reference Identification

This section provides the formal identification of this PP-Module per APE_INT requirements.

.PP-Module Identification
[cols="1,3"]
|===
|Attribute |Value

|PP-Module Title
|collaborative PP-Module for DBMS Cryptographic Functions

|PP-Module Short Name
|DBMS_MOD_CRYPTO

|PP-Module Version
|{revnumber}

|PP-Module Publication Date
|{revdate}

|PP-Module Sponsor
|{iTC-longname} international Technical Community ({iTC-shortname})

|CC Version
|CC:2022

|PP-Module Keywords
|Database, DBMS, Cryptography, TDE, BYOK, Key Management, Crypto Catalogue
|===

== PP-Module Base

This PP-Module requires the *collaborative Protection Profile for Database Management Systems (cPP_DBMS)* [<<cPP_DBMS>>] as its Base PP.

=== Allowed PP-Configurations

This PP-Module may be claimed in the following PP-Configurations:

. *cPP_DBMS + DBMS Cryptographic Module*
+
This standalone configuration is used when a DBMS requires cryptographic protections (data-at-rest encryption, key management, or key import) but is not deployed in a cloud environment.

. *cPP_DBMS + DBMS Cloud Module + DBMS Cryptographic Module*
+
This configuration is required when the DBMS Cloud Module [<<DBMS_Cloud_MOD>>] is claimed. The Cloud Module mandates the inclusion of this Crypto Module to provide Data-in-Transit (TLS/HTTPS) and Data-at-Rest encryption protections. See the DBMS Cloud Module for full PP-Configuration requirements.

=== PP-Module Relationship Diagram

[source]
----
                    +-------------------+
                    |    cPP_DBMS       |
                    |   (Base PP)       |
                    +--------+----------+
                             |
                             | Required Base
                             v
                    +-------------------+
                    | DBMS Crypto Module|
                    |  (This Document)  |
                    +-------------------+
                              ^
                              |  Required Dependency
                              |
                    +-------------------+
                    | DBMS Cloud Module |
                    |   (Optional)      |
                    +-------------------+
----

*Application Note:* When the DBMS Cloud Module is included in the PP-Configuration, this module is a mandatory dependency. When the DBMS Cloud Module is not included, this module may be claimed independently against the cPP_DBMS Base PP.

== TOE Overview

This PP-Module applies to the cryptographic functions of Database Management Systems (DBMS). It assumes the TOE implements cryptographic mechanisms for data protection.

=== TOE Type

The TOE type addressed by this PP-Module is: Database Management Systems implementing cryptographic protections for data-at-rest, key management, and/or secure key import.

=== TOE Scope

The TOE includes:

* Key management logic for data encryption keys (DEKs).
* Master key management functionality.
* Trusted channel interfaces for key import.

The TOE relies on the *Crypto Catalogue* [<<Crypto_Catalog>>] for the implementation of specific algorithms (AES, DRBG, etc.). The TOE explicitly excludes standalone cryptographic libraries or HSM appliances not integrated with the DBMS; those components are part of the operational environment.

== CC Conformance Claims

This section describes the conformance claims for this PP-Module per APE_CCL requirements defined in CC:2022.

=== Common Criteria Conformance

This PP-Module claims conformance to the Common Criteria for Information Technology Security Evaluation, CC:2022, as follows:

* *CC Part 1 Conformance*: This PP-Module is consistent with CC:2022 Part 1 [<<CC1>>].
* *CC Part 2 Conformance*: This PP-Module is CC Part 2 extended, as it includes extended SFR components defined in <<Extended Component Definitions>>.
* *CC Part 3 Conformance*: This PP-Module is CC Part 3 conformant, as it does not define extended SAR components.

=== Package Conformance

This PP-Module does not claim conformance to any pre-defined functional or assurance packages from CC:2022 Part 5.

=== PP-Module Conformance Type

This PP-Module requires *Exact Conformance* from Security Targets claiming conformance.

==== Definition of Exact Conformance

Exact Conformance is a subset of Strict Conformance as defined by CC:2022. A Security Target demonstrating Exact Conformance to this PP-Module must satisfy the following requirements:

. *Mandatory SFRs*: The ST shall include all SFRs specified in <<Security Functional Requirements (Mandatory)>> of this PP-Module.

. *No Omissions*: No mandatory SFRs from <<Security Functional Requirements (Mandatory)>> may be omitted from the ST.

. *Catalogue Claims*: The ST shall include the mandatory Crypto Catalogue component claims defined in <<Crypto Catalogue Claims>>.

. *No Additional SFRs*: The ST shall not include any SFRs from CC Part 2 [<<CC2>>] or extended component definitions not already included in this PP-Module or the Base PP (cPP_DBMS).

. *Iteration Permitted*: Iteration of SFRs is permitted to address multiple instances of a security requirement when necessary.

. *Operations*: All assignments, selections, and refinements shall be completed in accordance with the SFR specifications in this PP-Module and the Base PP.

=== Conformance Claim Rationale

A Security Target or PP claiming conformance to this PP-Module shall include:

. A statement identifying this PP-Module by title, version, and publication date.
. A statement that the claimed PP-Configuration includes the required Base PP (cPP_DBMS).
. A statement that the ST includes the mandatory Crypto Catalogue component claims as defined in <<Crypto Catalogue Claims>>.
. A statement that the ST demonstrates Exact Conformance to this PP-Module.

== Security Problem Definition

=== Threats

This section describes the threats that the TOE is designed to address. Each threat identifies the threat agent, the adverse action, and the asset being protected, in accordance with CC:2022 requirements.

T.KEY_COMPROMISE::
A malicious actor may obtain cryptographic keys (Master Keys or Data Encryption Keys) stored on the file system or in memory, enabling decryption of protected data. The threat agent may exploit insecure key storage, memory disclosure vulnerabilities, or unauthorized access to key management infrastructure. The assets at risk include Master Encryption Keys (MEKs), Data Encryption Keys (DEKs), and any data protected by those keys.

T.KEY_IMPORT_INTERCEPTION::
A malicious actor may intercept or modify cryptographic keys during import from an external entity (e.g., a Key Management System), compromising the key material before it reaches the TOE. The threat agent may exploit insecure transport protocols, missing authentication of the key management endpoint, or man-in-the-middle positions on the network. The assets at risk include imported Master Keys and the confidentiality and integrity of all data they subsequently protect.

T.WEAK_CRYPTO_IMPLEMENTATION::
The TOE may incorrectly apply cryptographic algorithms or key management logic, resulting in data exposure. The threat agent may exploit non-standard algorithm implementations, weak key sizes, insecure modes of operation, or deviations from established standards to recover plaintext data or keys. The assets at risk include all data protected by the cryptographic functions of the TOE.

=== Assumptions

This section describes the assumptions about the operational environment that must be satisfied for the TOE to provide its security functionality.

A.KEY_STORE_SECURITY::
The operational environment provides secure storage for cryptographic key material (e.g., hardware security modules, secure enclaves, or encrypted filesystems) such that keys are protected from unauthorized physical or logical access. The environment ensures that:
+
* Key material at rest cannot be extracted from the storage medium without proper authorization;
* Access to key storage is protected by authentication and access control mechanisms independent of the TOE;
* The integrity of stored key material is protected against unauthorized modification.

=== Organizational Security Policies

This section defines the organizational security policies applicable to the cryptographic functions of the TOE.

P.CRYPTOGRAPHIC_STANDARDS::
The organization shall require that the TOE implements cryptographic algorithms and key management practices in accordance with recognized standards. Specifically:
+
* Cryptographic algorithm selections shall comply with the requirements of the Common Criteria Cryptographic Catalogue [<<Crypto_Catalog>>];
* Key lengths and modes of operation shall meet the minimum requirements specified for the applicable assurance level;
* Deprecated or weak algorithms shall not be used for protecting sensitive data.
+
The TOE must provide mechanisms to configure algorithm selections in accordance with organizational policy.

P.KEY_LIFECYCLE::
The organization shall define and enforce a cryptographic key lifecycle policy that specifies:
+
* Procedures for key generation, including entropy requirements and approved generation methods;
* Key storage requirements, including protection of keys at rest;
* Key rotation schedules and procedures for re-encryption following rotation;
* Key destruction procedures to prevent unauthorized recovery of destroyed keys.
+
The TOE must support these lifecycle operations through the key management functions defined in this PP-Module.

== Security Objectives

This section defines the security objectives that address the threats, assumptions, and organizational security policies identified in the Security Problem Definition.

=== Security Objectives for the TOE

O.KEY_HIERARCHY::
The TOE shall enforce a key hierarchy where data encryption keys (DEKs) are generated and protected by a Master Key (MEK), ensuring that DEKs are never stored in plaintext outside the TOE boundary.

O.MASTER_KEY_MANAGEMENT::
The TOE shall provide functionality to generate, import, and destroy Master Keys securely, supporting "Bring Your Own Key" (BYOK) scenarios where keys originate from external trusted sources.

O.TRUSTED_KEY_IMPORT::
The TOE shall securely import cryptographic keys from external entities using trusted channels that provide confidentiality, integrity, and mutual authentication.

O.CATALOGUE_CONFORMANCE::
The TOE shall implement cryptographic algorithms in accordance with the requirements defined in the Common Criteria Cryptographic Catalogue [<<Crypto_Catalog>>].

=== Security Objectives for the Operational Environment

OE.KEY_STORE_PROTECTION::
The operational environment shall provide secure storage for cryptographic key material. Storage shall protect keys from unauthorized physical and logical access through mechanisms such as hardware security modules (HSMs), trusted platform modules (TPMs), secure enclaves, or encrypted filesystems that prevent unauthorized extraction of stored keys. The environment shall ensure keys at rest cannot be extracted without proper authorization and that the integrity of key material is maintained.

== Security Rationale

This section provides rationale showing how the defined security objectives address the identified threats and assumptions, and how the Security Functional Requirements (SFRs) satisfy the objectives. The rationale demonstrates completeness and consistency of the security problem definition, objectives, and requirements.

=== Threats to Objectives Mapping

The following table maps each threat to the security objectives that address it.

[cols="1,2,3",options="header"]
|===
| Threat
| Security Objectives Addressing the Threat
| Rationale

| T.KEY_COMPROMISE
| O.KEY_HIERARCHY +
  O.MASTER_KEY_MANAGEMENT
| Enforcing a key hierarchy (O.KEY_HIERARCHY) ensures that DEKs are always protected by the MEK and never stored in plaintext, limiting the impact of any single key compromise. Secure management of the MEK (O.MASTER_KEY_MANAGEMENT) prevents compromise of the root key from enabling bulk decryption.

| T.KEY_IMPORT_INTERCEPTION
| O.TRUSTED_KEY_IMPORT
| Establishing a trusted channel with confidentiality, integrity, and authentication controls (O.TRUSTED_KEY_IMPORT) prevents a malicious actor from intercepting or substituting key material during the BYOK import process.

| T.WEAK_CRYPTO_IMPLEMENTATION
| O.CATALOGUE_CONFORMANCE
| Requiring the TOE to implement only algorithms and key sizes that appear in the Common Criteria Cryptographic Catalogue (O.CATALOGUE_CONFORMANCE) ensures that only proven, standards-compliant implementations are used, eliminating the risk of custom or weak algorithm usage.

|===

=== Assumptions to Objectives Mapping

The following table maps each assumption to the operational environment objectives that uphold it.

[cols="1,2,3",options="header"]
|===
| Assumption
| OE Objectives Upholding the Assumption
| Rationale

| A.KEY_STORE_SECURITY
| OE.KEY_STORE_PROTECTION
| The operational environment objective OE.KEY_STORE_PROTECTION directly requires the environment to provide secure physical and logical protection of key material, which is the exact property assumed in A.KEY_STORE_SECURITY. This includes hardware security modules (HSMs), secure enclaves, or encrypted filesystems that prevent unauthorized extraction of stored keys.

|===

=== Objectives to SFRs Mapping

The following table maps each TOE security objective to the SFRs that satisfy it.

[cols="1,2,3",options="header"]
|===
| Security Objective for the TOE
| SFRs Satisfying the Objective
| Rationale

| O.KEY_HIERARCHY
| FCS_CKM.1/SKG +
  FDP_DAR_EXT.1 +
  *Crypto Catalogue: FCS_COP.1/SKC*
| FCS_CKM.1/SKG ensures DEKs are generated using approved, random methods. FDP_DAR_EXT.1 enforces that user data encryption uses DEKs consistent with the hierarchy. The Crypto Catalogue FCS_COP.1/SKC defines the algorithm used to encrypt data with those DEKs.

| O.MASTER_KEY_MANAGEMENT
| FCS_CKM_EXT.1 +
  *Crypto Catalogue: FCS_CKM.6*
| FCS_CKM_EXT.1 manages the full MEK lifecycle including generation, import, protection, and destruction. FCS_CKM.6 (from the Crypto Catalogue) specifies the key destruction method to ensure MEKs cannot be recovered after destruction.

| O.TRUSTED_KEY_IMPORT
| FDP_ITC_EXT.1 +
  *TLS FP / Crypto Catalogue: FCS_TLSC_EXT.1 or FCS_TLSS_EXT.1*
| FDP_ITC_EXT.1 defines the trusted channel requirements for key import, including confidentiality, integrity, and authentication properties. The channel implementation is satisfied by the Functional Package for TLS v2.1 [<<TLS_FP>>] or the applicable protocol component from the Crypto Catalogue [<<Crypto_Catalog>>].

| O.CATALOGUE_CONFORMANCE
| FCS_CKM.1/SKG +
  FDP_DAR_EXT.1 +
  FDP_ITC_EXT.1 +
  *Crypto Catalogue: All claimed components*
| Each SFR in this module that invokes a cryptographic algorithm explicitly requires the algorithm to be claimed from the Crypto Catalogue. FCS_CKM.1/SKG requires an approved DRBG; FDP_DAR_EXT.1 requires an approved symmetric cipher; FDP_ITC_EXT.1 requires an approved channel protocol. Together these ensure all cryptographic operations use only Catalogue-defined algorithms.

|===

=== Consistency Summary

==== Threat Coverage

All threats defined in the Security Problem Definition are addressed by at least one security objective:

* T.KEY_COMPROMISE: Addressed by O.KEY_HIERARCHY, O.MASTER_KEY_MANAGEMENT
* T.KEY_IMPORT_INTERCEPTION: Addressed by O.TRUSTED_KEY_IMPORT
* T.WEAK_CRYPTO_IMPLEMENTATION: Addressed by O.CATALOGUE_CONFORMANCE

==== Assumption Coverage

All assumptions are upheld by operational environment objectives:

* A.KEY_STORE_SECURITY: Upheld by OE.KEY_STORE_PROTECTION

==== Policy Coverage

All organizational security policies are addressed by security objectives:

* P.CRYPTOGRAPHIC_STANDARDS: Addressed by O.CATALOGUE_CONFORMANCE, which requires all algorithms to comply with the Crypto Catalogue
* P.KEY_LIFECYCLE: Addressed by O.KEY_HIERARCHY (generation), O.MASTER_KEY_MANAGEMENT (full MEK lifecycle), O.TRUSTED_KEY_IMPORT (import)

==== Objective Coverage

All TOE security objectives are satisfied by SFRs:

* O.KEY_HIERARCHY: FCS_CKM.1/SKG, FDP_DAR_EXT.1
* O.MASTER_KEY_MANAGEMENT: FCS_CKM_EXT.1
* O.TRUSTED_KEY_IMPORT: FDP_ITC_EXT.1
* O.CATALOGUE_CONFORMANCE: Satisfied collectively by Crypto Catalogue claims invoked across all SFRs

All OE objectives are either unconditional or have clear triggers linked to specific SFR selections.

== Security Functional Requirements

=== Conventions

The individual security functional requirements are specified in the sections below.
The following conventions are used for the completion of operations:

* [_Italicized text within square brackets_] indicates an operation to be completed by the ST author.

* *Bold text* indicates additional text provided as a refinement.

* [*Bold text within square brackets*] indicates the completion of an assignment.

* [text within square brackets] indicates the completion of a selection.

* Number in parentheses after SFR name, e.g. (1) indicates the completion of an iteration.

Extended SFRs are identified by having a label "EXT" at the end of the SFR name.

== Security Functional Requirements (Mandatory)

=== FCS: Cryptographic Support (Catalogue Invocation)

==== FCS_CKM.1/SKG Cryptographic Key Generation (Data Encryption Keys)

*FCS_CKM.1.1/SKG* The TSF shall generate cryptographic keys in accordance with a specified cryptographic key generation algorithm:

* *Key Generation Method*: [selection: Random Generation, Key Derivation]
* *Key Sizes*: [assignment: _list of supported key sizes (e.g., 128 bits, 256 bits)_]
* *Use Case*: Generation of Data Encryption Keys (DEKs) used for encrypting database data.

*Application Note:* This requirement uses the `FCS_CKM.1/SKG` iteration from CCDB-018 [<<Crypto_Catalog>>], which covers symmetric key generation. The Random Bit Generator used for generation must be claimed via `FCS_RBG.1` from the Crypto Catalogue.

*FCS_CKM.1.2/SKG* The TSF shall generate keys using a Random Bit Generator that complies with the requirements of the Crypto Catalogue [<<Crypto_Catalog>>].

===== Dependencies

[cols="1,2",options="header"]
|===
| Dependency | Resolution

| FCS_RBG.1 Random Bit Generation
| Satisfied by Crypto Catalogue claim (mandatory per <<Crypto Catalogue Claims>>). The ST author must claim the applicable DRBG component from the Crypto Catalogue.
|===

==== FCS_CKM_EXT.1 Cryptographic Key Management (Master Keys)

*FCS_CKM_EXT.1.1* The TSF shall manage the lifecycle of the Master/Root Cryptographic Keys (MEKs) as follows:

* *Key Origin*: [selection:
** *Generated Internally*: The TSF shall generate the Master Key using an algorithm claimed from the Crypto Catalogue and store it securely within the TOE boundary.
** *Imported from External Entity*: The TSF shall import the Master Key from an external IT entity via a Trusted Interface defined in FDP_ITC_EXT.1. *]

*FCS_CKM_EXT.1.2* The TSF shall protect the Master Key against disclosure and modification.

*FCS_CKM_EXT.1.3* The TSF shall destroy Master Keys in accordance with the key destruction requirements specified in the Crypto Catalogue (FCS_CKM.6).

*Application Note:* This requirement models the DBMS-specific behavior of managing a root key (e.g., TDE Master Key). When "Imported from External Entity" is selected, FDP_ITC_EXT.1 must be included in the ST.

===== Dependencies

[cols="1,2",options="header"]
|===
| Dependency | Resolution

| FCS_CKM.6 Cryptographic Key Destruction (Timing and Event)
| Satisfied by Crypto Catalogue claim (mandatory per <<Crypto Catalogue Claims>>). `FCS_CKM.6` is the key destruction component defined in CCDB-018 Section 3.7.

| FDP_ITC_EXT.1 Trusted Channel for Key Import (conditional)
| Satisfied by FDP_ITC_EXT.1 in this PP-Module. Required when "Imported from External Entity" is selected in FCS_CKM_EXT.1.1.
|===

=== FDP: User Data Protection

==== FDP_DAR_EXT.1 Data-at-Rest Encryption Strategy

*FDP_DAR_EXT.1.1* The TSF shall enforce a Data-at-Rest Encryption strategy as follows:

* *[selection:*
** *Full Volume Encryption*: The TSF shall encrypt all persistent user data using cryptographic algorithms claimed from the Crypto Catalogue.
** *Granular Data Encryption*: The TSF shall encrypt specific data elements (e.g., columns, cells) using cryptographic algorithms claimed from the Crypto Catalogue. *]*

*FDP_DAR_EXT.1.2* The TOE shall ensure that the encryption of data adheres to the algorithm and mode requirements specified in the Crypto Catalogue (e.g., AES-CBC, AES-GCM, AES-XTS).

*Application Note:* The ST author must select the encryption strategy appropriate for the TOE and claim the applicable Symmetric Encryption component from the Crypto Catalogue per <<Crypto Catalogue Claims>>.

===== Dependencies

[cols="1,2",options="header"]
|===
| Dependency | Resolution

| FCS_COP.1/SKC Cryptographic Operation (Symmetric-Key Cryptography)
| Satisfied by Crypto Catalogue claim (mandatory per <<Crypto Catalogue Claims>>). The ST author must claim `FCS_COP.1/SKC` from CCDB-018. If an authenticated encryption mode is used (e.g., AES-GCM), `FCS_COP.1/AEAD` must also be claimed.

| FCS_CKM_EXT.1 Cryptographic Key Management (Master Keys)
| Satisfied by FCS_CKM_EXT.1 in this PP-Module, which manages the DEK protection hierarchy.
|===

==== FDP_ITC_EXT.1 Trusted Channel for Key Import

*FDP_ITC_EXT.1.1* The TSF shall be capable of establishing a trusted channel between itself and an external key management entity for the purpose of importing cryptographic keys.

*FDP_ITC_EXT.1.2* The TSF shall ensure the trusted channel provides:

* *Confidentiality*: Protection against disclosure.
* *Integrity*: Protection against modification.
* *Authentication*: [selection:
** *Mutual Authentication*: The TOE shall authenticate the external entity, and the external entity shall authenticate the TOE (e.g., using certificates).
** *Server Authentication*: The TOE shall authenticate the external entity. *]

*FDP_ITC_EXT.1.3* The trusted channel shall be implemented using a protocol claimed from the Crypto Catalogue or the Functional Package for TLS v2.1 [<<TLS_FP>>].

*Application Note:* This SFR is required when "Imported from External Entity" is selected in FCS_CKM_EXT.1.1. The Functional Package for TLS v2.1 [<<TLS_FP>>] is the preferred mechanism for satisfying the channel protocol requirement.

===== Dependencies

[cols="1,2",options="header"]
|===
| Dependency | Resolution

| FCS_TLSC_EXT.1 / FCS_TLSS_EXT.1 (when TLS channel selected)
| Satisfied by Functional Package for TLS v2.1 [<<TLS_FP>>] or applicable Crypto Catalogue protocol component. The ST author must claim the applicable TLS component.
|===

=== FCS/FDP Class Dependencies Summary

.FCS/FDP Class Dependency Resolution
[cols="1,2,2",options="header"]
|===
| SFR | Dependencies | Resolution

| FCS_CKM.1/SKG
| FCS_RBG.1 (CCDB-018 Section 6.2)
| Crypto Catalogue (mandatory claim per <<Crypto Catalogue Claims>>)

| FCS_CKM_EXT.1
| FCS_CKM.6 (CCDB-018 Section 3.7); FDP_ITC_EXT.1 (conditional)
| Crypto Catalogue (FCS_CKM.6); FDP_ITC_EXT.1 (this module, when import selected)

| FDP_DAR_EXT.1
| FCS_COP.1/SKC (CCDB-018 Section 4.10); FCS_COP.1/AEAD (if authenticated encryption, CCDB-018 Section 4.2); FCS_CKM_EXT.1
| Crypto Catalogue (FCS_COP.1/SKC and/or FCS_COP.1/AEAD); FCS_CKM_EXT.1 (this module)

| FDP_ITC_EXT.1
| FCS_TLSC_EXT.1 / FCS_TLSS_EXT.1 (conditional)
| Functional Package for TLS v2.1 [<<TLS_FP>>] or Crypto Catalogue (when TLS channel selected)
|===

== Security Assurance Requirements (SARs)

This PP-Module does not define additional Security Assurance Requirements beyond those already specified by the collaborative Protection Profile for Database Management Systems (cPP_DBMS).

All SARs defined in the cPP_DBMS apply directly and fully to TOEs claiming conformance with this DBMS Cryptographic Functions Module.

=== SAR Inheritance

The following SAR families, as defined in the cPP_DBMS, are inherited unchanged:

* ADV: Development
* AGD: Guidance Documents
* ALC: Life-cycle Support
* ASE: Security Target Evaluation
* ATE: Tests
* AVA: Vulnerability Assessment

No additional or modified SARs are introduced by this PP-Module.

[appendix]
== Crypto Catalogue Claims

This appendix defines the mandatory selection of components from the Crypto Catalogue that the TOE must claim to support the operations defined in this module. The ST author must include all mandatory claims; conditional claims apply when the corresponding SFR selection is made.

=== Mandatory Catalogue Claims

The Security Target *shall* include the following components from the Crypto Catalogue:

. *Symmetric-Key Cryptography*: To satisfy `FDP_DAR_EXT.1` and support DEK-based data encryption.
+
CCDB-018 Component: `FCS_COP.1/SKC` (Symmetric-Key Cryptography) for modes such as AES-CBC, AES-GCM, or AES-XTS. +
Note: `FCS_COP.1/AEAD` (Authenticated Encryption with Associated Data) should additionally be claimed if an authenticated encryption mode (e.g., AES-GCM) is used.

. *Random Bit Generation*: To satisfy `FCS_CKM.1/SKG` key generation requirements.
+
CCDB-018 Component: `FCS_RBG.1` (Random Bit Generation, Section 6.2)

. *Hashing*: To satisfy integrity checks for key wrapping and import verification.
+
CCDB-018 Component: `FCS_COP.1/Hash` (Cryptographic Operation – Hashing, Section 4.4) using SHA-256 or stronger

. *Key Destruction*: To satisfy `FCS_CKM_EXT.1.3`.
+
CCDB-018 Component: `FCS_CKM.6` (Timing and Event of Cryptographic Key Destruction, Section 3.7)

=== Conditional Catalogue Claims

The Security Target *shall* include the following components when the indicated SFR selection is made:

[cols="1,2,2",options="header"]
|===
| Condition | Required Catalogue Component | Satisfying SFR

| "Imported from External Entity" selected in FCS_CKM_EXT.1.1
| TLS client or server components from Functional Package for TLS v2.1 [<<TLS_FP>>] or Crypto Catalogue
| FDP_ITC_EXT.1

| "Mutual Authentication" selected in FDP_ITC_EXT.1.2
| Asymmetric algorithm component (e.g., FCS_COP.1/SigGen) for certificate-based authentication
| FDP_ITC_EXT.1

|===

=== Evaluation Note

The evaluator shall verify that the ST includes the applicable components from the Crypto Catalogue. The evaluator shall apply the Assurance Activities defined within the Crypto Catalogue for those components. The Evaluation Activities defined in this module's SD are supplementary and focus on DBMS-specific logic (Key Hierarchy, Encryption Strategy) not covered by the Catalogue.

[appendix]
== Optional Requirements

There are no Optional Requirements for this PP-Module.

[appendix]
== Extended Component Definitions

This appendix contains the formal definitions for the extended requirements used in this PP-Module, in accordance with the conventions of CC:2022 Part 2 [<<CC2>>].

(Note: formatting conventions for selections and assignments in this appendix follow those in CC:2022 Part 2 [<<CC2>>].)

=== FCS: Cryptographic Support

==== FCS_CKM_EXT: Cryptographic Key Management (Master Keys)

===== Family Behaviour

This family defines requirements for the TSF to manage the lifecycle of Master/Root Cryptographic Keys (MEKs) used to protect Data Encryption Keys (DEKs) in a DBMS key hierarchy. This family is distinct from the standard FCS_CKM family, which addresses general key generation and destruction, in that it explicitly addresses the DBMS-specific concept of a root key that wraps DEKs, and the "Bring Your Own Key" (BYOK) scenario where the master key originates from an external trusted source.

===== Component Levelling

FCS_CKM_EXT.1 Cryptographic Key Management (Master Keys) requires the TSF to manage the full lifecycle of the Master Encryption Key (MEK), including generation or import, protection, and destruction.

===== Management: FCS_CKM_EXT.1

The following actions could be considered for the management functions in FMT:

a) Selection of Master Key origin (internal generation vs. external import).
b) Initiation of Master Key rotation.
c) Initiation of Master Key destruction.
d) Configuration of secure key storage settings.

===== Audit: FCS_CKM_EXT.1

The following actions should be auditable if FAU_GEN Security audit data generation is included in the PP/ST:

a) Basic: Generation of a Master Key.
b) Basic: Import of a Master Key from an external entity.
c) Basic: Destruction of a Master Key.
d) Detailed: Key rotation operations.

===== FCS_CKM_EXT.1 Cryptographic Key Management (Master Keys)

Hierarchical to: No other components.

Dependencies: `FCS_CKM.6` Timing and Event of Cryptographic Key Destruction (CCDB-018 §3.7); `FDP_ITC_EXT.1` Trusted Channel for Key Import (this module, conditional when import selected).

*FCS_CKM_EXT.1.1* The TSF shall manage the lifecycle of the Master/Root Cryptographic Keys (MEKs) as follows:

* *Key Origin*: [selection:
** *Generated Internally*: The TSF shall generate the Master Key using an algorithm claimed from the Crypto Catalogue and store it securely within the TOE boundary.
** *Imported from External Entity*: The TSF shall import the Master Key from an external IT entity via a Trusted Interface defined in FDP_ITC_EXT.1. *]

*FCS_CKM_EXT.1.2* The TSF shall protect the Master Key against disclosure and modification.

*FCS_CKM_EXT.1.3* The TSF shall destroy Master Keys in accordance with the key destruction requirements specified in the Crypto Catalogue (FCS_CKM.6).

=== FDP: User Data Protection

==== FDP_DAR_EXT: Data-at-Rest Encryption Strategy

===== Family Behaviour

This family defines requirements for the TSF to enforce a data-at-rest encryption strategy for persistent database storage. This family is distinct from generic storage protection requirements in that it explicitly addresses the scope of encryption (Full Volume vs. Granular) and the relationship to the key hierarchy managed by FCS_CKM_EXT.1. This allows the ST author to precisely characterize how broadly encryption is applied within the DBMS, which is a key differentiator between different DBMS product architectures (e.g., Transparent Data Encryption vs. column-level encryption).

===== Component Levelling

FDP_DAR_EXT.1 Data-at-Rest Encryption Strategy requires the TSF to define and enforce the scope and method of encryption applied to persistent database data.

===== Management: FDP_DAR_EXT.1

The following actions could be considered for the management functions in FMT:

a) Selection of encryption strategy (Full Volume vs. Granular).
b) Configuration of which data elements are subject to Granular Encryption (when selected).
c) Configuration of encryption algorithm settings (in conjunction with Crypto Catalogue selections).

===== Audit: FDP_DAR_EXT.1

The following actions should be auditable if FAU_GEN Security audit data generation is included in the PP/ST:

a) Basic: Enabling or disabling data-at-rest encryption.
b) Basic: Changes to the encryption scope or strategy.
c) Detailed: Encryption or decryption failures.

===== FDP_DAR_EXT.1 Data-at-Rest Encryption Strategy

Hierarchical to: No other components.

Dependencies: `FCS_COP.1/SKC` Cryptographic Operation – Symmetric-Key Cryptography (CCDB-018 §4.10); `FCS_COP.1/AEAD` Cryptographic Operation – Authenticated Encryption with Associated Data (CCDB-018 §4.2, when an authenticated encryption mode is used); `FCS_CKM_EXT.1` Cryptographic Key Management (Master Keys) (this module).

*FDP_DAR_EXT.1.1* The TSF shall enforce a Data-at-Rest Encryption strategy as follows:

* *[selection:*
** *Full Volume Encryption*: The TSF shall encrypt all persistent user data using cryptographic algorithms claimed from the Crypto Catalogue.
** *Granular Data Encryption*: The TSF shall encrypt specific data elements (e.g., columns, cells) using cryptographic algorithms claimed from the Crypto Catalogue. *]*

*FDP_DAR_EXT.1.2* The TOE shall ensure that the encryption of data adheres to the algorithm and mode requirements specified in the Crypto Catalogue (e.g., AES-CBC, AES-GCM, AES-XTS).

==== FDP_ITC_EXT: Trusted Channel for Key Import

===== Family Behaviour

This family defines requirements for the TSF to establish a trusted channel to an external key management entity for the purpose of securely importing cryptographic key material. This family focuses specifically on the BYOK key import use case and complements FCS_CKM_EXT.1 by defining the communication channel properties required when the "Imported from External Entity" selection is made. It is distinct from the general trusted channel families (FTP_ITC, FTP_TRP) in that it is scoped to key import operations and explicitly defines the authentication requirements in the context of key management.

===== Component Levelling

FDP_ITC_EXT.1 Trusted Channel for Key Import requires the TSF to establish a trusted channel that provides confidentiality, integrity, and authentication for key import operations.

===== Management: FDP_ITC_EXT.1

The following actions could be considered for the management functions in FMT:

a) Configuration of trusted external key management entity endpoints.
b) Management of certificates or credentials used for channel authentication.
c) Selection of authentication mode (mutual vs. server-only).

===== Audit: FDP_ITC_EXT.1

The following actions should be auditable if FAU_GEN Security audit data generation is included in the PP/ST:

a) Basic: Establishment of a trusted channel to an external key management entity.
b) Basic: Successful or failed key import operations.
c) Basic: Authentication failures on the trusted channel.
d) Detailed: Certificate validation results during channel establishment.

===== FDP_ITC_EXT.1 Trusted Channel for Key Import

Hierarchical to: No other components.

Dependencies: `FCS_TLSC_EXT.1` and/or `FCS_TLSS_EXT.1` from the Functional Package for TLS v2.1 [<<TLS_FP>>] when TLS is selected as the channel protocol. These components are defined in the TLS Functional Package and must be claimed in the ST when the TLS channel is selected.

*FDP_ITC_EXT.1.1* The TSF shall be capable of establishing a trusted channel between itself and an external key management entity for the purpose of importing cryptographic keys.

*FDP_ITC_EXT.1.2* The TSF shall ensure the trusted channel provides:

* *Confidentiality*: Protection against disclosure.
* *Integrity*: Protection against modification.
* *Authentication*: [selection:
** *Mutual Authentication*: The TOE shall authenticate the external entity, and the external entity shall authenticate the TOE (e.g., using certificates).
** *Server Authentication*: The TOE shall authenticate the external entity. *]

*FDP_ITC_EXT.1.3* The trusted channel shall be implemented using a protocol claimed from the Crypto Catalogue or the Functional Package for TLS v2.1 [<<TLS_FP>>].

[appendix]
== Consistency Rationale

=== Consistency of Threats and Objectives

.Consistency Rationale for Threats
[%header, cols=".^1,.^2"]
|===
|Threat
|Consistency Rationale

|T.KEY_COMPROMISE
|This threat is addressed by O.KEY_HIERARCHY, which ensures DEKs are always protected by the MEK and never stored in plaintext, limiting the impact of any single DEK compromise. O.MASTER_KEY_MANAGEMENT complements this by securing the root key through its full lifecycle, ensuring that compromise of the MEK itself is prevented through proper generation, storage, and destruction controls.

|T.KEY_IMPORT_INTERCEPTION
|This threat is addressed by O.TRUSTED_KEY_IMPORT, which requires a channel providing confidentiality (preventing interception of key material), integrity (preventing modification of key material in transit), and authentication (preventing substitution of the key management endpoint) during all BYOK import operations.

|T.WEAK_CRYPTO_IMPLEMENTATION
|This threat is addressed by O.CATALOGUE_CONFORMANCE, which requires all cryptographic algorithm implementations to be drawn from the Common Criteria Cryptographic Catalogue. This ensures only validated, standards-compliant algorithms and key sizes are used within the TOE, eliminating the risk of proprietary or non-standard implementations.

|===

=== Consistency of Assumptions and Objectives

.Consistency Rationale for Assumptions
[%header, cols=".^1,.^2"]
|===
|Assumption
|Consistency Rationale

|A.KEY_STORE_SECURITY
|This assumption is upheld by OE.KEY_STORE_PROTECTION, which requires the operational environment to provide secure physical and logical protection for cryptographic key material. The TOE is entitled to rely on this protection to ensure that keys stored outside the immediate TOE execution boundary (e.g., on-disk storage, HSMs) remain protected. The TOE complements this by protecting keys while they are actively in use within the TOE boundary, as specified by FCS_CKM_EXT.1.

|===

=== Consistency of Objectives and SFRs

.Consistency Rationale for TOE Objectives
[%header, cols=".^1,.^2"]
|===
|TOE Objective
|Consistency Rationale

|O.KEY_HIERARCHY
|This objective is satisfied by FCS_CKM.1/SKG, which ensures DEKs are generated using approved random methods, and FDP_DAR_EXT.1, which enforces that all persistent user data is encrypted using those DEKs. The Crypto Catalogue FCS_COP.1/SKC component defines the approved algorithm used to perform the DEK-based encryption, completing the hierarchy. Together these SFRs ensure that plaintext data is never stored without DEK protection and that DEKs are always generated through approved entropy sources.

|O.MASTER_KEY_MANAGEMENT
|This objective is satisfied by FCS_CKM_EXT.1, which manages the full MEK lifecycle including generation or import, protection from disclosure and modification, and destruction. The Crypto Catalogue FCS_CKM.6 component defines the approved key destruction method to ensure MEKs cannot be recovered after destruction. FCS_CKM_EXT.1 explicitly covers the BYOK scenario by requiring import to be performed via a trusted channel (FDP_ITC_EXT.1) when external key origin is selected.

|O.TRUSTED_KEY_IMPORT
|This objective is satisfied by FDP_ITC_EXT.1, which defines the trusted channel requirements for BYOK key import, including confidentiality, integrity, and authentication. The channel implementation is provided by TLS components from the Functional Package for TLS v2.1 [<<TLS_FP>>] or the Crypto Catalogue, ensuring the channel itself uses approved protocols. This prevents any key import operation from being performed over an unauthenticated or unprotected channel.

|O.CATALOGUE_CONFORMANCE
|This objective is satisfied collectively by all SFRs in this module that invoke the Crypto Catalogue. FCS_CKM.1/SKG requires an approved DRBG; FDP_DAR_EXT.1 requires approved symmetric encryption; FDP_ITC_EXT.1 requires an approved channel protocol; FCS_CKM_EXT.1 invokes FCS_CKM.6 for destruction. The mandatory Catalogue Claims appendix ensures the ST author explicitly selects the required components, and the evaluator verifies those selections using Catalogue-defined Assurance Activities.

|===

.Consistency Rationale for Environmental Objectives
[%header, cols=".^1,.^2"]
|===
|Environmental Objective
|Consistency Rationale

|OE.KEY_STORE_PROTECTION
|This objective is an operational environment requirement that directly upholds A.KEY_STORE_SECURITY. It is not satisfied by TOE SFRs; the TOE relies on the platform to provide secure key storage outside the TOE execution boundary. TOE SFRs (FCS_CKM_EXT.1) complement this by protecting keys while they are actively in use within the TOE boundary, but the persistent protection of stored key material is an environmental responsibility.

|===

=== Consistency of Organizational Security Policies and Objectives

.Consistency Rationale for Organizational Security Policies
[%header, cols=".^1,.^2"]
|===
|OSP
|Consistency Rationale

|P.CRYPTOGRAPHIC_STANDARDS
|This policy is supported by O.CATALOGUE_CONFORMANCE, which directly requires conformance to the Common Criteria Cryptographic Catalogue. All SFRs in this module that invoke cryptographic operations reference the Catalogue explicitly, and the mandatory Catalogue Claims appendix enforces the required algorithm selections in the ST. The evaluator verifies Catalogue compliance using defined Assurance Activities, ensuring the policy is enforced through the evaluation process itself.

|P.KEY_LIFECYCLE
|This policy is supported by O.KEY_HIERARCHY (covering DEK generation via FCS_CKM.1/SKG and use via FDP_DAR_EXT.1), O.MASTER_KEY_MANAGEMENT (covering MEK generation, protection, and destruction via FCS_CKM_EXT.1, with FCS_CKM.6 ensuring approved destruction), and O.TRUSTED_KEY_IMPORT (covering the key import phase of the lifecycle via FDP_ITC_EXT.1). Together these objectives and their associated SFRs address the complete key lifecycle required by the policy: generation, storage protection, use, rotation support, and destruction.

|===

[appendix]
== SFR List

This table is provided as a reference of all SFRs included in this PP-Module.

The Type column has the following definitions:

Mandatory:: The requirement must be included in the ST.
Selection-Based:: The requirement must be included in the ST when selections in other SFRs trigger its inclusion, or when the TOE implements the specified functionality.
Optional:: The requirement may be included in the ST at the ST author's discretion.

.Security Functional Requirements
[%header,cols=".^3,.^6,.^2"]
|===

|Requirement Class
|Requirement Component
|Type

.2+|Cryptographic Support (FCS)
|FCS_CKM.1/SKG Cryptographic Key Generation (Data Encryption Keys)
|Mandatory

|FCS_CKM_EXT.1 Cryptographic Key Management (Master Keys)
|Mandatory

.2+|User Data Protection (FDP)
|FDP_DAR_EXT.1 Data-at-Rest Encryption Strategy
|Mandatory

|FDP_ITC_EXT.1 Trusted Channel for Key Import
|Mandatory

|===

*Application Note on FDP_DIT_EXT.1:* The Data-in-Transit Protection requirement (`FDP_DIT_EXT.1`) is defined in the DBMS Cloud Module [<<DBMS_Cloud_MOD>>] and carries a dependency on Crypto Catalogue TLS components. When the DBMS Cloud Module is included in the PP-Configuration and TLS or HTTPS is selected in `FDP_DIT_EXT.1`, the ST author must claim the applicable TLS SFRs from the Functional Package for TLS v2.1 [<<TLS_FP>>] or the Crypto Catalogue. These channel SFRs are not independently defined in this module but are invoked by Cloud Module SFRs.

=== SFR Dependency Summary

.SFR Dependencies
[%header,cols=".^3,.^4,.^4"]
|===
|SFR
|Dependencies
|Resolution

|FCS_CKM.1/SKG
|FCS_RBG.1 (CCDB-018 §6.2, Random Bit Generation)
|Crypto Catalogue claim (mandatory per <<Crypto Catalogue Claims>>)

|FCS_CKM_EXT.1
|FCS_CKM.6 (CCDB-018 §3.7, Timing and Event of Key Destruction) +
FDP_ITC_EXT.1 (when import selected)
|FCS_CKM.6: Crypto Catalogue claim; FDP_ITC_EXT.1: this module (conditional)

|FDP_DAR_EXT.1
|FCS_COP.1/SKC (CCDB-018 §4.10) and/or FCS_COP.1/AEAD (CCDB-018 §4.2) +
FCS_CKM_EXT.1
|FCS_COP.1/SKC, FCS_COP.1/AEAD: Crypto Catalogue claims; FCS_CKM_EXT.1: this module

|FDP_ITC_EXT.1
|FCS_TLSC_EXT.1 and/or FCS_TLSS_EXT.1 (when TLS channel selected)
|Functional Package for TLS v2.1 [<<TLS_FP>>] or Crypto Catalogue

|===

[appendix]
== Global Dependency Resolution Summary

This section provides a comprehensive view of all SFR dependencies and their resolutions for this PP-Module.

=== Dependency Resolution by Type

.Dependencies Satisfied by Module SFRs
[cols="1,2,2",options="header"]
|===
| Dependent SFR | Required Dependency | Satisfying SFR

| FCS_CKM_EXT.1 (when import selected)
| FDP_ITC_EXT.1
| FDP_ITC_EXT.1 (this module)

| FDP_DAR_EXT.1
| FCS_CKM_EXT.1
| FCS_CKM_EXT.1 (this module)

|===

.Dependencies Satisfied by Crypto Catalogue
[cols="1,2,2",options="header"]
|===
| Dependent SFR | Required Dependency | Catalogue Component

| FCS_CKM.1/SKG
| FCS_RBG.1
| CCDB-018 §6.2: `FCS_RBG.1` Random Bit Generation (supports DRBG per NIST SP 800-90A)

| FCS_CKM_EXT.1
| FCS_CKM.6
| CCDB-018 §3.7: `FCS_CKM.6` Timing and Event of Cryptographic Key Destruction

| FDP_DAR_EXT.1
| FCS_COP.1/SKC; FCS_COP.1/AEAD (if authenticated encryption used)
| CCDB-018 §4.10: `FCS_COP.1/SKC` (AES-CBC, AES-XTS); CCDB-018 §4.2: `FCS_COP.1/AEAD` (AES-GCM)

|===

.Dependencies Satisfied by Functional Package for TLS v2.1 or Crypto Catalogue (Conditional)
[cols="1,2,2",options="header"]
|===
| Dependent SFR | Required Dependency | Resolution

| FDP_ITC_EXT.1 (when TLS channel selected)
| FCS_TLSC_EXT.1, FCS_TLSS_EXT.1
| Functional Package for TLS v2.1 [<<TLS_FP>>] or Crypto Catalogue

|===

=== Catalogue Invocation Rationale

This PP-Module deliberately does not define SFRs for specific cryptographic algorithm implementations (AES, SHA, DRBG, RSA, etc.). All such requirements are satisfied by selecting components from the Common Criteria Cryptographic Catalogue [<<Crypto_Catalog>>] for the following reasons:

. *Separation of Concerns:* The Crypto Catalogue is maintained by dedicated cryptographic experts and reflects current standards (e.g., FIPS 140-3, NIST SP 800-series). Defining algorithms here would duplicate that work and risk divergence over time as standards evolve.

. *Flexibility:* Different deployments may require different algorithm suites. The Catalogue selection mechanism allows ST authors to choose the appropriate suite (e.g., Suite B, commercial algorithms) without modifying this module.

. *Reusability:* Catalogue components can be shared across multiple PP-Modules and PP-Configurations, promoting consistency across the DBMS iTC's document family and reducing the total evaluation burden.

. *FIPS/CAVP Alignment:* Separating algorithm requirements into Catalogue claims facilitates alignment with FIPS 140-3 validation and CAVP testing, as evaluators apply Catalogue-defined Assurance Activities directly to those components.

[appendix]
== Glossary

For the purpose of this PP-Module, the following terms and definitions apply. Terms defined in CC:2022 Part 1 [<<CC1>>] are included where they have particular relevance to this PP-Module.

[glossary]
BYOK (Bring Your Own Key)::
A key management model in which the customer or tenant supplies their own cryptographic master key to the TOE, rather than allowing the TOE or the cloud provider to generate it. The customer retains control over the master key lifecycle, including generation, rotation, and destruction. BYOK is enabled in this module by FCS_CKM_EXT.1 (import selection) and FDP_ITC_EXT.1 (trusted channel).

Crypto Catalogue::
The Common Criteria Cryptographic Catalogue, a repository of standardized SFR components for cryptographic algorithm implementations maintained by the Common Criteria community. This module invokes Catalogue components rather than defining algorithm-specific SFRs directly, ensuring alignment with current standards.

Data Encryption Key (DEK)::
A cryptographic key used to encrypt and decrypt user data at rest. DEKs are generated by the TOE (via `FCS_CKM.1/SKG`) and are protected by the Master Encryption Key (MEK). DEKs should not be stored in plaintext outside the TOE boundary.

Full Volume Encryption::
An encryption strategy in which all persistent user data stored by the DBMS is encrypted, regardless of data classification or sensitivity. This strategy provides uniform protection but may have broader performance implications.

Granular Data Encryption::
An encryption strategy in which specific data elements (e.g., individual columns, rows, or cells) are selectively encrypted by the DBMS based on data classification, policy, or sensitivity. This allows finer-grained access control but requires more complex key management.

Key Management System (KMS)::
An external system or service that generates, stores, and manages cryptographic keys on behalf of the TOE. In BYOK scenarios, the KMS holds the master key and provides it to the TOE via a trusted channel (FDP_ITC_EXT.1). Examples include hardware security modules (HSMs), cloud-native KMS services (e.g., AWS KMS, Azure Key Vault), and on-premises key management appliances.

Master Encryption Key (MEK)::
The root cryptographic key in the DBMS key hierarchy. The MEK protects (wraps) Data Encryption Keys (DEKs). Compromise of the MEK could enable decryption of all data protected by the DEKs it wraps. MEK lifecycle management is governed by FCS_CKM_EXT.1.

TDE (Transparent Data Encryption)::
A database encryption feature that automatically encrypts data before it is written to storage and decrypts it when read, transparently to the application. TDE typically implements a key hierarchy with a master key protecting DEKs. This module's FDP_DAR_EXT.1 and FCS_CKM_EXT.1 address the security requirements for TDE implementations.

[appendix]
== Acronyms

.Acronyms
[%header,cols="1,4"]
|===

|Acronym
|Meaning

|AES
|Advanced Encryption Standard

|BYOK
|Bring Your Own Key

|CAVP
|Cryptographic Algorithm Validation Program

|CC
|Common Criteria

|cPP
|collaborative Protection Profile

|DBMS
|Database Management System

|DEK
|Data Encryption Key

|DRBG
|Deterministic Random Bit Generator

|FIPS
|Federal Information Processing Standard

|HSM
|Hardware Security Module

|HTTPS
|Hypertext Transfer Protocol Secure

|iTC
|international Technical Community

|KEK
|Key Encryption Key

|KMS
|Key Management System

|MEK
|Master Encryption Key

|NIAP
|National Information Assurance Partnership

|NIST
|National Institute of Standards and Technology

|OSP
|Organizational Security Policy

|PP
|Protection Profile

|RBG
|Random Bit Generator

|RSA
|Rivest–Shamir–Adleman (asymmetric algorithm)

|SAR
|Security Assurance Requirement

|SFR
|Security Functional Requirement

|SHA
|Secure Hash Algorithm

|SPD
|Security Problem Definition

|ST
|Security Target

|TDE
|Transparent Data Encryption

|TLS
|Transport Layer Security

|TOE
|Target of Evaluation

|TPM
|Trusted Platform Module

|TSF
|TOE Security Functionality

|XTS
|XEX-based Tweaked CodeBook mode with ciphertext Stealing

|===
